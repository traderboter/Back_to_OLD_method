🚀 مستندات جامع روند اجرای ربات معاملاتی - سیستم جدید (معماری ماژولار)
================================================================

📌 توضیح کلی:
این مستندات روند کامل اجرای ربات معاملاتی ارز دیجیتال با معماری جدید ماژولار را توضیح می‌دهد.

🔑 تفاوت‌های کلیدی سیستم جدید با سیستم قدیم:
1. **معماری ماژولار signal_generation**: به جای یک فایل signal_generator.py، اکنون یک پکیج کامل با 90+ ماژول
2. **SignalOrchestrator**: کنترل‌کننده اصلی که تمام Analyzer ها را هماهنگ می‌کند
3. **IndicatorCalculator جداگانه**: محاسبات اندیکاتورها جدا از SignalGenerator
4. **11 Analyzer تخصصی**: Trend, Momentum, Volume, Pattern, SR, Volatility, Harmonic, Channel, Cyclical, HTF, VolumePattern
5. **29 الگوی کندل استیک**: الگوهای پیشرفته با کلاس‌های جداگانه
6. **5 الگوی چارت**: Double Top/Bottom, Flag/Pennant, Head & Shoulders, Triangle, Wedge

================================================================
🎯 جریان کلی اجرا
================================================================

مرحله ۱: شروع برنامه → if __name__ == "__main__"
مرحله ۲: پردازش آرگومان‌های خط فرمان
مرحله ۳: بارگذاری فایل تنظیمات
مرحله ۴: به‌روزرسانی تنظیمات از خط فرمان
مرحله ۵: راه‌اندازی سیستم لاگینگ
مرحله ۶: پشتیبان‌گیری اولیه
مرحله ۷: اعمال تغییرات command-line
مرحله ۸: ایجاد نمونه ربات
مرحله ۹: تنظیم هندلرهای سیگنال سیستم
مرحله ۱۰: اجرای ربات و جریان اصلی

================================================================
📝 توضیحات مرحله به مرحله
================================================================

مرحله ۱: شروع برنامه
اتفاقات:

برنامه از بلوک if __name__ == "__main__" شروع می‌شود (خط 366)
تابع async main() با asyncio.run() اجرا می‌شود (خط 370)

مرحله ۲: پردازش آرگومان‌های خط فرمان (خط 168-186)
ساخته می‌شود:

یک ArgumentParser برای دریافت پارامترهای خط فرمان
پارامترهای قابل دریافت:

-c/--config: مسیر فایل تنظیمات (پیش‌فرض: config.yaml)
-v/--verbose: فعال‌سازی لاگ جزئیات
--symbols: لیست نمادهای معاملاتی
--strategy: انتخاب استراتژی خاص
--backup: ایجاد نسخه پشتیبان
--no-trading: حالت شبیه‌سازی
--no-watch-config: غیرفعال کردن نظارت بر تغییرات کانفیگ
--update-config: به‌روزرسانی بخشی از تنظیمات

مرحله ۳: بارگذاری فایل تنظیمات (خط 189-210)
فراخوانی: load_config(args.config)

اتفاقات:

خواندن فایل تنظیمات (YAML یا JSON)
ثبت زمان آخرین تغییر فایل
اضافه کردن بخش config_management در صورت عدم وجود
ایجاد دایرکتوری data (storage directory)
خطاها: در صورت عدم وجود یا خطا در فایل، برنامه با کد خروج 1 متوقف می‌شود

مرحله ۴: به‌روزرسانی تنظیمات از خط فرمان (خط 212-248)
اگر --update-config فراخوانی شود:

پارس کردن مقدار JSON
به‌روزرسانی یا ایجاد بخش در config
ذخیره‌سازی تغییرات در فایل تنظیمات

مرحله ۵: راه‌اندازی سیستم لاگینگ (خط 251)
فراخوانی: setup_logging(config, args.verbose)

ساخته می‌شود:

Console Handler برای نمایش لاگ‌ها در ترمینال
File Handler (اختیاری) با یا بدون قابلیت چرخش فایل
تنظیمات:

سطح لاگ (DEBUG/INFO/WARNING/ERROR)
فرمت لاگ
حداکثر سایز فایل و تعداد نسخه پشتیبان

مرحله ۶: پشتیبان‌گیری اولیه (خط 257-286)
اگر --backup فراخوانی شود:

ساخته می‌شود:

یک فایل ZIP شامل:
config.yaml
trades.db
adaptive_learning_data.json
correlation_data.json
performance_metrics.json
مسیر ذخیره: data/backups/manual_backup_[timestamp].zip

مرحله ۷: اعمال تغییرات command-line (خط 289-299)
اتفاقات:

اگر --no-trading: تنظیم حالت شبیه‌سازی
اگر --symbols: تنظیم لیست نمادهای معاملاتی

مرحله ۸: ایجاد نمونه ربات (خط 302-322)
فراخوانی: bot_instance = CryptoTradingBot(args.config)

در کلاس CryptoTradingBot.__init__ اتفاقات زیر می‌افتد:

8.1 ایجاد ConfigurationManager
مدیریت بارگذاری و تغییرات تنظیمات
8.2 مقداردهی متغیرهای اولیه
self.exchange_client = None
self.data_fetcher = None
self.signal_generator = None  # در واقع SignalOrchestrator
self.signal_processor = None
self.trade_manager = None
self.performance_tracker = None
self.backup_manager = None
self.strategy_manager = None
# توجه: indicator_calculator در initialize_components ایجاد می‌شود نه در __init__
8.3 تنظیم وضعیت اجرا
ایجاد instance_id یکتا
ثبت زمان شروع
جمع‌آوری اطلاعات سیستم (OS, Platform, Python version)
8.4 ثبت listener برای تغییرات کانفیگ
config_manager.register_update_listener(self._handle_config_changes)

مرحله ۹: تنظیم هندلرهای سیگنال سیستم (خط 325-332)
ثبت می‌شود:

SIGINT (Ctrl+C)
SIGTERM (kill command)
هدف: خروج تمیز و ذخیره داده‌ها قبل از بستن

================================================================
مرحله ۱۰: اجرای ربات (خط 345)
================================================================

فراخوانی: await bot_instance.run()

🔧 جریان اجرای متد run() در CryptoTradingBot
================================================================

مرحله 10.1: راه‌اندازی کامپوننت‌ها (خط 2437 تقریبی)
فراخوانی: await self.initialize_components()

کامپوننت‌های راه‌اندازی شده به ترتیب:

1. TradingStrategyManager (خط 1679-1687)
   - مدیریت استراتژی‌های مختلف معاملاتی
   - انتخاب کانفیگ بر اساس استراتژی فعال

2. TradingBrainAI (خط 1691-1703)
   - هوش مصنوعی معاملاتی
   - مدل‌های یادگیری ماشین

3. ExchangeClient (خط 1706-1734)
   - from exchange_client import ExchangeClient
   - self.exchange_client = ExchangeClient(self.config)
   - await self.exchange_client._init_session()
   - ایجاد نشست HTTP/WebSocket برای ارتباط با صرافی

4. MarketDataFetcher (خط 1737-1766)
   - from market_data_fetcher import MarketDataFetcher
   - self.data_fetcher = MarketDataFetcher(self.config, self.exchange_client)
   - await self.data_fetcher.initialize()
   - دریافت داده‌های بازار (قیمت، حجم، OHLCV)
   - مدیریت کش

5. ⭐ IndicatorCalculator (خط 1768-1770) - جدید در سیستم ماژولار
   - from signal_generation.shared.indicator_calculator import IndicatorCalculator
   - self.indicator_calculator = IndicatorCalculator(self.config)
   - محاسبه تمام اندیکاتورهای تکنیکال به صورت جداگانه:
     * RSI (Relative Strength Index)
     * MACD (Moving Average Convergence Divergence)
     * EMA (Exponential Moving Average)
     * SMA (Simple Moving Average)
     * Bollinger Bands
     * ATR (Average True Range)
     * ADX (Average Directional Index)
     * Stochastic Oscillator
     * OBV (On-Balance Volume)
   - کش کردن نتایج برای بهینه‌سازی عملکرد

6. ⭐ SignalOrchestrator (خط 1773-1831) - معماری جدید ماژولار
   - from signal_generation.orchestrator import SignalOrchestrator as SignalGenerator
   - self.signal_generator = SignalGenerator(
       self.config,
       self.data_fetcher,
       self.indicator_calculator  # ⭐ پارامتر جدید
     )

   📊 معماری داخلی SignalOrchestrator:

   A. 11 Analyzer تخصصی (signal_generation/analyzers/):

      1. TrendAnalyzer: تحلیل روند (صعودی، نزولی، خنثی)
         - تشخیص روند با EMA/SMA
         - قدرت روند با ADX
         - تأیید با ساختار قیمتی

      2. MomentumAnalyzer: تحلیل مومنتوم
         - RSI (Relative Strength Index)
         - Stochastic Oscillator
         - تشخیص واگرایی‌ها

      3. VolumeAnalyzer: تحلیل حجم معاملات
         - OBV (On-Balance Volume)
         - Volume Profile
         - Accumulation/Distribution

      4. ⭐ PatternAnalyzer: تشخیص الگوها (signal_generation/analyzers/patterns/)

         📍 الگوهای کندل استیک (29 الگو):
         • Doji (Doji, Dragonfly, Gravestone, Long-Legged)
         • Star Patterns (Morning Star, Evening Star, Morning Doji Star, Evening Doji Star)
         • Reversal Patterns (Hammer, Hanging Man, Inverted Hammer, Shooting Star)
         • Engulfing Patterns (Bullish/Bearish Engulfing)
         • Harami Patterns (Harami, Harami Cross)
         • Three-Pattern Series (Three White Soldiers, Three Black Crows, Three Inside, Three Outside, Three Methods)
         • Other Patterns (Piercing Line, Dark Cloud Cover, Belt Hold, Kicking, Marubozu, Mat Hold, Tweezer, Spinning Top, Abandoned Baby)

         📍 الگوهای چارت (5 الگو):
         • Double Top/Bottom
         • Head & Shoulders (معمولی و معکوس)
         • Triangle (صعودی، نزولی، متقارن)
         • Wedge (صعودی، نزولی)
         • Flag & Pennant

      5. SRAnalyzer: تحلیل حمایت و مقاومت
         - تشخیص سطوح کلیدی
         - Pivot Points
         - Fibonacci Retracements

      6. VolatilityAnalyzer: تحلیل نوسان
         - ATR (Average True Range)
         - Bollinger Bands Width
         - Standard Deviation

      7. HarmonicAnalyzer: الگوهای هارمونیک
         - Gartley, Butterfly, Bat
         - ABCD Pattern
         - Fibonacci Ratios

      8. ChannelAnalyzer: کانال‌های قیمتی
         - Parallel Channels
         - Regression Channels
         - Donchian Channels

      9. CyclicalAnalyzer: تحلیل چرخه‌ای
         - Market Cycles
         - Seasonal Patterns
         - Time-based Analysis

      10. HTFAnalyzer: تحلیل تایم‌فریم‌های بالاتر
          - Alignment بین تایم‌فریم‌ها
          - HTF Trend Confirmation
          - Multi-TF Divergence

      11. VolumePatternAnalyzer: الگوهای حجمی پیشرفته
          - Volume Breakouts
          - Volume Divergence
          - Smart Money Flow

   B. سیستم‌های پشتیبان (signal_generation/systems/):

      • MarketRegimeDetector: تشخیص رژیم بازار (صعودی، نزولی، رنج)
      • AdaptiveLearningSystem: یادگیری تطبیقی از معاملات گذشته
      • CorrelationManager: مدیریت همبستگی نمادها
      • EmergencyCircuitBreaker: توقف اضطراری در شرایط بحرانی

   C. پایپ‌لاین پردازش سیگنال:

      1. دریافت داده → MarketDataFetcher
      2. محاسبه اندیکاتورها → IndicatorCalculator
      3. اجرای 11 Analyzer به صورت موازی
      4. ترکیب نتایج → SignalScorer
      5. اعتبارسنجی → SignalValidator
      6. ارسال سیگنال نهایی

   D. مدیریت تایم‌فریم‌ها:

      • Multi-Timeframe Analysis
      • Timeframe Weight Configuration
      • Higher Timeframe Confirmation
      • Timeframe Score Caching

7. MLSignalIntegration (خط 1834-1859)
   - self.ml_integration = MLSignalIntegration(
       signal_generator=self.signal_generator,
       trading_brain=self.trading_brain
     )
   - یکپارچه‌سازی سیگنال‌ها با هوش مصنوعی
   - تقویت سیگنال‌ها با ML

8. BotPerformanceTracker (خط 1862-1864)
   - self.performance_tracker = BotPerformanceTracker(self.config, self.db_path)
   - ردیابی متریک‌های عملکرد
   - ذخیره در: data/performance_metrics.json

9. BackupManager (خط 1867-تقریبی)
   - self.backup_manager = BackupManager(self.config)
   - پشتیبان‌گیری خودکار دوره‌ای

10. TradeManager (خط 1872-1956)
    - from trade_manager import TradeManager
    - self.trade_manager = TradeManager(self.config, self.db_path)
    - self.trade_manager.initialize_db()
    - ثبت کالبک‌های جدید (خط 1949-1953)
    - مدیریت معاملات
    - اجرای خرید/فروش
    - مدیریت Take Profit و Stop Loss
    - ذخیره معاملات در: data/trades.db (SQLite)

11. SignalProcessor (خط 1958-2000)
    - from signal_processor import SignalProcessor
    - self.signal_processor = SignalProcessor(
        config=self.config,
        market_data_fetcher=self.data_fetcher,
        orchestrator=self.signal_generator,  # توجه: در کد orchestrator نامیده می‌شود
        ml_integration=self.ml_integration
      )
    - await self.signal_processor.initialize()
    - ثبت کالبک TradeManager (خط 1996)
    - پردازش دوره‌ای سیگنال‌ها
    - ارسال سیگنال‌ها به TradeManager

12. دریافت نمادهای فعال
    - await self._fetch_active_symbols()
    - لیست نمادهای معاملاتی از صرافی

================================================================
مرحله 10.2: آموزش مدل‌های AI
================================================================

فراخوانی: await self._train_ai_models()

اتفاقات:

دریافت داده‌های تاریخی (معمولاً BTC/USDT، 1 سال)
آماده‌سازی داده (Feature Engineering)
آموزش مدل‌های Ensemble (Random Forest, XGBoost, LSTM)
آموزش Position Sizer (اختیاری)
داده‌ها ذخیره می‌شوند در:
  - مدل‌های آموزش‌دیده در حافظه
  - وزن‌های مدل (اگر فعال باشد)

================================================================
مرحله 10.3: شروع سرویس‌های پس‌زمینه
================================================================

فراخوانی: await self.start_services()

سرویس‌های شروع شده:

1. به‌روزرسانی دوره‌ای قیمت‌ها
   - await self.trade_manager.start_periodic_price_update()
   - بررسی قیمت‌های جاری هر 10 ثانیه
   - به‌روزرسانی وضعیت معاملات فعال

2. پردازش دوره‌ای سیگنال‌ها
   - await self.signal_processor.start_periodic_processing()
   - تولید و بررسی سیگنال‌های جدید
   - فواصل زمانی متغیر بر اساس تعداد نمادها

3. پشتیبان‌گیری خودکار
   - await self.backup_manager.start_automated_backup()
   - پشتیبان‌گیری دوره‌ای (پیش‌فرض: هر 24 ساعت)
   - ذخیره در: data/backups/backup_[timestamp].zip

4. نظارت بر تغییرات تنظیمات
   - self._config_check_task = asyncio.create_task(self._config_watch_loop())
   - بررسی تغییرات در config.yaml هر 30 ثانیه
   - بارگذاری مجدد خودکار

5. ایجاد پشتیبان اولیه

================================================================
مرحله 10.4: حلقه اصلی ربات
================================================================

حلقه while تا زمان دریافت سیگنال توقف:

while not self._shutdown_requested.is_set():
    # 1. به‌روزرسانی زمان کارکرد
    self.running_status['uptime_seconds'] = ...

    # 2. بررسی سلامت کامپوننت‌ها
    await self._check_component_health()

    # 3. به‌روزرسانی نمادهای فعال (هر 30 دقیقه)
    if self.running_status['uptime_seconds'] % 1800 < 10:
        await self._fetch_active_symbols()

    # 4. انتظار 10 ثانیه
    await asyncio.sleep(10)

بررسی سلامت شامل (خط 2519-2582):

وضعیت اتصال به صرافی (ExchangeClient)
وضعیت DataFetcher
وضعیت SignalProcessor
وضعیت TradeManager
وضعیت نظارت بر تنظیمات (Config Watcher)
راه‌اندازی مجدد خودکار در صورت خطا

================================================================
📊 جریان داده و ارتباطات بین کامپوننت‌ها (معماری جدید)
================================================================

┌─────────────────────┐
│  ExchangeClient     │ ◄── ارتباط با صرافی
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ MarketDataFetcher   │ ◄── دریافت داده‌های بازار (OHLCV)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ IndicatorCalculator │ ◄── ⭐ محاسبه اندیکاتورها (RSI, MACD, EMA,...)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────────────┐
│  SignalOrchestrator (SignalGenerator)       │
│  ┌─────────────────────────────────────┐   │
│  │ 11 Parallel Analyzers:               │   │
│  │ • TrendAnalyzer                      │   │
│  │ • MomentumAnalyzer                   │   │
│  │ • VolumeAnalyzer                     │   │
│  │ • PatternAnalyzer (34 patterns)      │   │
│  │ • SRAnalyzer                         │   │
│  │ • VolatilityAnalyzer                 │   │
│  │ • HarmonicAnalyzer                   │   │
│  │ • ChannelAnalyzer                    │   │
│  │ • CyclicalAnalyzer                   │   │
│  │ • HTFAnalyzer                        │   │
│  │ • VolumePatternAnalyzer              │   │
│  └─────────────────────────────────────┘   │
│                    │                         │
│                    ▼                         │
│  ┌─────────────────────────────────────┐   │
│  │ Support Systems:                     │   │
│  │ • MarketRegimeDetector               │   │
│  │ • AdaptiveLearningSystem             │   │
│  │ • CorrelationManager                 │   │
│  │ • EmergencyCircuitBreaker            │   │
│  └─────────────────────────────────────┘   │
│                    │                         │
│                    ▼                         │
│  ┌─────────────────────────────────────┐   │
│  │ • SignalScorer → Combine Results    │   │
│  │ • SignalValidator → Validate Signal │   │
│  └─────────────────────────────────────┘   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
         ┌─────────────────┐
    ┌───►│ TradingBrainAI  │
    │    └─────────────────┘
    │            │
    │            ▼
┌───┴────────────────────┐
│ MLSignalIntegration    │ ◄── تقویت سیگنال با ML
└──────────┬─────────────┘
           │
           ▼
┌─────────────────────┐
│  SignalProcessor    │ ◄── پردازش و فیلتر سیگنال‌ها
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   TradeManager      │ ◄── اجرای معاملات + TP/SL
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  ExchangeClient     │ ◄── ارسال سفارش
└─────────────────────┘

================================================================
💾 فایل‌ها و داده‌های ایجاد شده
================================================================

1. پایگاه داده
   مسیر: data/trades.db
   محتوا:
   - معاملات (trades)
   - موقعیت‌های فعال (positions)
   - تاریخچه سفارشات
   - نتایج معاملات

2. فایل‌های JSON
   performance_metrics.json:
   {
     "daily_metrics": {...},
     "system_stats": {
       "uptime_seconds": 3600,
       "restart_count": 1,
       "error_count": 0
     }
   }

   adaptive_learning_data.json:
   - نتایج یادگیری تطبیقی
   - امتیازات الگوها
   - تنظیمات بهینه

   correlation_data.json:
   - همبستگی بین نمادها
   - موقعیت‌های فعال

3. فایل‌های پشتیبان
   مسیر: data/backups/
   فرمت: backup_YYYYMMDD_HHMMSS.zip

4. فایل‌های لاگ (اختیاری)
   مسیر: تنظیم شده در config
   محتوا: تمام رویدادها، خطاها، معاملات

================================================================
🔄 فرآیندهای دوره‌ای در حال اجرا
================================================================

| فرآیند | فاصله زمانی | توضیح |
|--------|-------------|--------|
| به‌روزرسانی قیمت | 10 ثانیه | بررسی قیمت‌ها و شرایط TP/SL |
| پردازش سیگنال‌ها* | متغیر (3-15 دقیقه) | پردازش هوشمند سیگنال‌ها بر اساس تعداد نمادها |
| بررسی کانفیگ | 30 ثانیه | نظارت بر تغییرات فایل تنظیمات |
| به‌روزرسانی نمادها | 30 دقیقه | دریافت لیست نمادهای فعال از صرافی |
| پشتیبان‌گیری | 24 ساعت | ایجاد نسخه پشتیبان از داده‌ها |
| بررسی سلامت | 10 ثانیه | بررسی وضعیت کامپوننت‌ها |

* پردازش سیگنال‌ها: فاصله زمانی به صورت پویا و هوشمند تنظیم می‌شود:
  - کمتر از 20 نماد: هر 3 دقیقه (180 ثانیه)
  - 20-50 نماد: هر 5 دقیقه (300 ثانیه)
  - 50-100 نماد: هر 10 دقیقه (600 ثانیه)
  - بیش از 100 نماد: هر 15 دقیقه (900 ثانیه)
  - تمام تایم‌فریم‌ها (5m, 15m, 1h, 4h) به صورت همزمان پردازش می‌شوند
  - منطق پردازش: signal_processor.py خطوط تقریبی 1037-1093

================================================================
🛑 خاموش شدن و پاکسازی
================================================================

وقتی Ctrl+C زده می‌شود یا سیگنال SIGTERM دریافت می‌شود:

1. ثبت سیگنال و فراخوانی bot_instance.stop()
2. تنظیم پرچم _shutdown_requested
3. خروج از حلقه اصلی
4. فراخوانی await self.shutdown():
   - ایجاد پشتیبان نهایی
   - ذخیره متریک‌های عملکرد
   - توقف تمام سرویس‌های پس‌زمینه
   - بستن اتصال به صرافی
   - بستن پایگاه داده
   - لغو تسک‌های asyncio
5. خروج با کد مناسب (0 برای موفق، 1 برای خطا)

================================================================
📋 مقایسه معماری جدید vs قدیم
================================================================

| ویژگی | سیستم قدیم | سیستم جدید (ماژولار) |
|--------|------------|---------------------|
| SignalGenerator | تک فایل (~3000 خط) | Orchestrator + 90+ ماژول |
| Analyzers | 6 تحلیلگر | 11 تحلیلگر تخصصی |
| الگوهای کندل | محدود | 29 الگو با کلاس‌های جداگانه |
| الگوهای چارت | 2-3 الگو | 5 الگوی پیشرفته |
| IndicatorCalculator | درون SignalGenerator | کلاس جداگانه و مستقل |
| Maintainability | متوسط | بسیار بالا (ماژولار) |
| Extensibility | محدود | بسیار بالا |
| Testing | دشوار | آسان (Unit Tests) |
| Performance | خوب | بهینه (کش، موازی‌سازی) |

================================================================
🎓 نکات مهم برای توسعه‌دهندگان
================================================================

1. **اضافه کردن Analyzer جدید:**
   - کلاس جدید در signal_generation/analyzers/
   - ارث‌بری از BaseAnalyzer
   - ثبت در orchestrator.py

2. **اضافه کردن الگوی جدید:**
   - کندل استیک: signal_generation/analyzers/patterns/candlestick/
   - چارت: signal_generation/analyzers/patterns/chart/
   - ارث‌بری از BasePattern

3. **اضافه کردن اندیکاتور جدید:**
   - signal_generation/analyzers/indicators/
   - ارث‌بری از BaseIndicator
   - ثبت در IndicatorOrchestrator

4. **تست کردن:**
   - هر ماژول قابل تست جداگانه است
   - Unit tests برای هر Analyzer
   - Integration tests برای Orchestrator

================================================================
📞 اطلاعات بیشتر
================================================================

- مسیر کد اصلی: Back_to_OLD_method/
- مسیر signal_generation: Back_to_OLD_method/signal_generation/
- فایل اصلی: main.py
- کلاس اصلی ربات: crypto_trading_bot.py
- Orchestrator: signal_generation/orchestrator.py
- تنظیمات: config.yaml

================================================================
✅ پایان مستندات
================================================================

نسخه: 2.0 (معماری ماژولار)
آخرین به‌روزرسانی: 2025
نویسنده: Claude Code
