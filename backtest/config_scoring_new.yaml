# ================= Scoring Config: NEW SYSTEM =================
# این فایل فقط شامل تنظیمات scoring است
# برای استفاده در backtest با روش NEW
# ================================================================

# ================= Orchestrator Settings =================
orchestrator:
  # ✅ NEW SYSTEM: Best Signal Selection (not aggregation)
  use_multi_tf_aggregation: false

signal_processing:
  scoring:
    # ✅ NEW SYSTEM - سیستم جدید
    scoring_method: new

    # تنظیمات OLD غیرفعال
    old_system:
      symbol_performance_enabled: false
      correlation_safety_enabled: false
      structure_score_enabled: false
      harmonic_separate_multiplier: false
      channel_separate_multiplier: false
      cyclical_separate_multiplier: false

      # Alignment-based confluence
      use_rr_based_confluence: false

      # محدود به 300
      max_final_score: 300

    # وزن‌های analyzer (NEW defaults)
    weights:
      trend: 0.3
      momentum: 0.25
      volume: 0.2
      patterns: 0.1
      support_resistance: 0.08
      volatility: 0.05
      harmonic: 0.01
      channel: 0.005
      cyclical: 0.003
      htf: 0.002

    # وزن‌های timeframe (NEW defaults)
    timeframe_weights:
      5m: 0.7
      15m: 0.85
      1h: 1.0
      4h: 1.2

    # پارامترهای امتیازدهی
    base_score_scale: 100
    max_confluence_bonus: 0.5
    htf_alignment_bonus: 0.3
    htf_misalignment_penalty: 0.3

# تنظیمات Analyzers
analyzers:
  volume:
    require_4h_confirmation: true  # 🆕 اجباری بودن تأیید حجم در 4h

# Validation thresholds برای NEW
validation:
  min_signal_score: 60         # حداقل 60 از 300
  strong_signal_threshold: 150  # قوی > 150
  min_rr_ratio: 2.0
  max_risk_percent: 2.0
